<!DOCTYPE html>
<html>
<head>

</head>
<body>
    {% block content %}
        <h1>Welcome, {{ user.getUsername() }}</h1>
        <img src="{{ user.getRandomImage() }}" alt="Profile Image">
    {% endblock %}
</body>
</html>
